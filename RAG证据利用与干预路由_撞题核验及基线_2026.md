# RAG“证据利用”与“干预路由”撞题核验及基线

更新日期：2026-07-22

## 1. 最终判定

### 撞题等级：Level 2——高重叠，但仍可收窄为可毕业的实证章节

两个原始表述都不是凭空构造的问题，但也都不能按“首次提出”来写：

1. **“证据到达不等于证据利用”是已有明确术语、指标和受控实验的成熟问题。** RAGChecker 已定义 `context utilization`；Blinded by Generated Contexts 和 2026 年的 CDD 都用受控冲突或因果干预研究模型是否真正服从外部上下文。
2. **“修订、回滚、重启和风险门控”已有非常接近的工作。** Doctor-RAG 已做失败诊断、最早错误点定位、按错误类型选择局部修复动作；RetroRAG 已做回溯式证据与推理更新；CDD-α 已做选择性干预门控；AlignRAG 已做检索证据约束的测试时修订。
3. 因而，**局部修复本身不是新意，风险门控本身也不是新意**。尚能成立的增量是：在失败尚未被最终答案揭示时，固定中间生成状态、查询和检索文档，实际执行 KEEP、APPEND、REVISE/RESTART，得到成对结果监督；再训练轻量路由器，以“收益同时限制正确→错误风险”为目标选择动作。

建议正式名称改为：

> **面向动态 RAG 的风险校准检索后干预路由**

不建议继续把“Counterfactual/反事实”放在标题中心。ICLR 2026 的 CF-RAG 已经用这个词表示“生成反事实查询以区分因果证据与相关证据”，容易造成概念冲突。本文的标签来自每个动作的真实配对 rollout，更准确的称呼是“paired-outcome supervision（成对结果监督）”。

## 2. 可辩护的一句话增量

> 不同于 Doctor-RAG 在完整轨迹已经失败后诊断并修复，本文在失败尚未知时，对同一中间状态、同一查询和同一检索证据实际执行 KEEP、APPEND 与 REVISE/RESTART，用成对结果监督学习轻量路由，并显式约束正确答案被干预改错的风险。

这个增量适合硕士毕业论文的一章；按当前文献密度，不宜承诺强论文级方法创新。

## 3. 原始主张的四轴拆解

| 轴 | 受控诊断 | 风险校准干预路由 |
|---|---|---|
| 问题 | 检索命中支持证据后，生成器仍可能不使用 | 追加证据、重启或修订的效果因样本而异，并可能污染原本正确答案 |
| 机制 | 固定状态、查询、文档和解码，仅改变证据注入/干预算子 | 预测每个动作的结果增益与伤害概率，再选择 KEEP/APPEND/REVISE |
| 监督 | 同一样本、同一状态下不同动作的实际终局 F1/Accuracy | 成对 rollout 标签；LoRA-SFT 分类或排序，不依赖 RL |
| 评测 | 到达率、利用率、成对增益、前缀锚定效应 | F1/EM、错→对、对→错、风险—覆盖曲线、token/时延成本 |

## 4. 最直接的前人工作和可用基线

### 4.1 证据到达与证据利用

#### RAGChecker（NeurIPS 2024，直接测量基线）

- 把 RAG 错误拆成检索器与生成器的细粒度诊断。
- 生成器指标包含 faithfulness、噪声敏感性、hallucination、self-knowledge 和 **context utilization**。
- `context utilization` 衡量“已能由检索上下文支持的金标 claim 中，实际出现在回答里的比例”。
- 这是第一点最应采用的直接指标基线，且有开源代码。
- 来源：[NeurIPS 论文页](https://proceedings.neurips.cc/paper_files/paper/2024/hash/27245589131d17368cccdfa990cbf16e-Abstract-Datasets_and_Benchmarks_Track.html)，[代码](https://github.com/amazon-science/RAGChecker)

#### Blinded by Generated Contexts（ACL 2024，受控归因基线）

- 构造“模型生成上下文”与“外部检索上下文”相冲突的设置，并控制哪一侧包含正确答案。
- 发现模型即使面对正确外部证据，仍可能偏向自己生成的上下文；问题相似度和 chunk 切分会影响利用。
- 它为“生成前缀锚定导致外部证据未被利用”提供了直接前人依据。
- 来源：[ACL Anthology](https://aclanthology.org/2024.acl-long.337/)

#### Does RAG Know When Retrieval Is Wrong? / CDD（arXiv 2026，最新直接诊断基线）

- 用 Context-Driven Decomposition 分离上下文答案与参数知识答案，再通过错误注入、截断等干预测试上下文服从性。
- Epi-Scale 含 HotpotQA、NQ、FEVER 的 clean/adversarial 配对数据，扰动包括实体替换、时间偏移、逻辑矛盾和干扰证据。
- Standard RAG 在 adversarial 条件明显下降；CDD 提升 adversarial 表现，却损失 clean 表现。论文报告的 clean/adversarial 调和均值中，两者几乎持平，恰好说明“干预会伤害干净样本”。
- CDD-α 用阈值门控选择性干预，是风险路由必须比较的提示式基线。
- 局限：截至 2026-07-22 仍是 arXiv 预印本；实验主要依赖闭源模型，门控不是训练或校准后的概率模型。
- 来源：[arXiv 摘要](https://arxiv.org/abs/2605.14473)，[PDF](https://arxiv.org/pdf/2605.14473)

### 4.2 回溯修订、局部修复与动作路由

#### Doctor-RAG（arXiv 2026，最接近的方法基线）

- 对已经失败的完整 Agentic-RAG 轨迹诊断证据是否充分、错误类型和最早失败点。
- 按 Format、Reasoning、Retriever、Search 错误选择工具条件修复；推理错误时保留正确前缀，从最早失败点重新推理。
- 用教师生成诊断/修复数据，对 Qwen3-8B 或 Llama-3.1-8B 做 SFT，算力路径与本课题 LoRA-SFT 很接近。
- 它基本覆盖了“诊断 + 操作选择 + 前缀复用/局部修复”，因此不能再宣称这些组件本身新颖。
- 本课题剩余差异：Doctor-RAG 以“已知失败的完整轨迹”为输入；本课题在终局未知的中间状态做 KEEP/APPEND/REVISE 的预防性决策，并显式约束干预伤害。
- 来源：[arXiv PDF](https://arxiv.org/pdf/2604.00865)

#### RetroRAG / Evidence Retroactivity（arXiv 2025，回溯更新基线）

- 指出单向、不可逆推理难以吸收后到证据；通过证据整理、发现和回答模块迭代更新证据与推理节点。
- 在 HotpotQA、2WikiMultiHopQA 上比较 Standard、CoT、RAG、ReAct、Self-Ask、IR-CoT、SearChain、MetaRAG。
- 可作为无需训练的“允许后到证据回溯修改推理”系统基线。
- 与本课题的差异：它没有固定同一中间状态与同一证据来估计各干预算子的个体收益和伤害。
- 来源：[arXiv PDF](https://arxiv.org/pdf/2501.05475)

#### AlignRAG（NeurIPS 2025，证据约束修订基线）

- 训练 8B critic 生成对比式批评，并在测试时迭代修订完整回答；AlignRAG-auto 动态决定何时停止。
- 是“检索证据驱动的自我修订”强基线，但作用对象是完整回答，不是严格的中间状态动作选择。
- 可以直接复用公开模型/代码做推理对比，避免在本课题里重训其 critic。
- 来源：[NeurIPS 论文页](https://proceedings.neurips.cc/paper_files/paper/2025/hash/1f09e1ee5035a4c3fe38a5681cae5815-Abstract-Conference.html)，[代码](https://github.com/upup-wei/RAG-ReasonAlignment)

#### Astute RAG（ACL 2025，冲突整合基线）

- 先引出模型内部知识，再迭代整合内部与外部证据，并按来源可靠性形成答案。
- 适合作为训练免费的冲突感知整合基线。
- 与本课题的差异：它不在同状态上比较 KEEP/APPEND/REVISE 的实际潜在结果。
- 来源：[ACL Anthology](https://aclanthology.org/2025.acl-long.1476/)

### 4.3 风险与选择性干预

#### CDD-α（2026，最直接的门控基线）

- 当提示式冲突分数超过阈值才启动复杂分解。
- 应实现为零训练基线，并在同一验证集上调阈值；不能只与“总是干预”比较。

#### PragAURA（ICLR 2026 投稿后撤稿，仅作方法参考）

- 同时研究检索资源分配与校准拒答，采用风险—覆盖和 conformal calibration 思路。
- 因为已撤稿，不宜作为论文论证的核心前人工作；可借用其风险—覆盖评测形式。
- 来源：[OpenReview](https://openreview.net/forum?id=aosmeS01n8)

#### GuarantRAG（ACL 2026，概念强但不适合主复现）

- 生成内部答案和参考答案，再用对比式 DPO 与联合解码促进知识整合。
- 说明“保证知识整合”已有正式工作；但训练成本和 DPO 路径不符合本课题算力约束。
- 只作为概念上界或相关工作，不作为必须复现的主基线。
- 来源：[ACL Anthology](https://aclanthology.org/2026.acl-long.192/)

### 4.4 “反事实”术语冲突

#### CF-RAG（ICLR 2026）

- 通过生成并评估反事实查询，区分因果证据与表面相关证据，再进行并行仲裁。
- 它与本课题机制不同，但已经占用了“Counterfactual Reasoning for RAG”的命名空间。
- 因此本文应称“成对结果监督”或“受控干预”，避免让评审误以为研究因果证据识别。
- 来源：[OpenReview](https://openreview.net/forum?id=9U51rOnGko)

## 5. 与本地 ETC 思路的关系

本地已有结果不是新方向的替代品，而是对学界问题的一个具体、可控落点：

| 观察 | 本地证据 | 对应学界问题 |
|---|---:|---|
| 支持句到达后经常没有收益 | 181 个可改进动作中，70 个命中 gold 支持句，仅 14 个正收益 | RAGChecker 的 context utilization；Blinded/CDD 的上下文服从 |
| 相同真实 BM25 证据，restart 优于 append | 首次 ETC 状态 oracle：`+0.26243` 对 `+0.12037` | RetroRAG/Doctor-RAG 的回溯修订动机 |
| restart 也会把正确答案改错 | 真实 restart 有 2 次正确→错误 | CDD clean/adversarial 权衡与选择性干预 |
| 只换检索时机收益小 | 密集时机诊断 oracle 仅约 `+0.025 F1` | 说明本地章节更适合研究“利用/干预”，而不是再做一个 timing 方法 |

因此，**学界方向比 ETC 更宽；ETC 只是一个可复现的状态提取器和第一批实验载体**。论文不能写成“改进 ETC”，而应写成：在 ETC、DRAGIN 等动态 RAG 产生的中间状态上，诊断证据利用并路由检索后干预。第一阶段可用 ETC 完成协议，第二阶段迁移到 DRAGIN 验证不是 ETC 特例。

## 6. 基线矩阵：哪些必须跑，哪些只引用

### 必须跑：低成本、能回答核心问题

1. 无检索/原始 RAG。
2. ETC 原始 APPEND。
3. DRAGIN 原始动态检索。
4. 固定首次触发 + KEEP、APPEND、FULL-RESTART。
5. 新增 LOCAL-REVISE（回退到最近句界或最近未被证据支持的 claim）。
6. RAGChecker 的 context utilization 指标。
7. CDD-α 风格阈值门控。
8. 规则路由器：证据不足/冲突/当前答案置信度阈值。
9. LoRA-SFT 风险路由器。
10. Oracle：按真实终局分数选最佳动作，只作为可达上界。

### 建议跑：有公开实现或提示即可复现

1. RetroRAG 的训练免费流程。
2. Astute RAG 提示流程。
3. AlignRAG 的公开 critic 推理。
4. Doctor-RAG 的简化复现：最早失败点 + 固定错误类型路由；若官方代码发布则改用官方实现。

### 只引用，不作为硬性复现

1. GuarantRAG：需要 DPO/联合解码，超出算力预算。
2. 闭源大模型版本 CDD：可复现提示逻辑，不追求完全复现其闭源数字。
3. PragAURA：已撤稿，只借鉴校准评测。

## 7. 最接近论文的重叠对比

| 工作 | 问题相同 | 状态/证据受控 | 多干预算子 | 风险门控 | 训练预算 | 与本文剩余差异 |
|---|---|---|---|---|---|---|
| RAGChecker | 是 | 部分 | 否 | 否 | 无需训练 | 指标，不做动态动作选择 |
| Blinded 2024 | 是 | 是 | 上下文来源干预 | 否 | 无需训练 | 不学习在线路由 |
| CDD 2026 | 是 | 是 | 分解/截断 | CDD-α | 提示式 | 不做 KEEP/APPEND/REVISE 配对监督 |
| Doctor-RAG 2026 | 高度相同 | 轨迹级 | 是 | 隐式 | 8B SFT | 已知失败后修复，不限制未知终局时的干预伤害 |
| RetroRAG 2025 | 相邻 | 否 | 回溯更新 | 否 | 无需训练 | 无个体动作收益/伤害预测 |
| AlignRAG 2025 | 相邻 | 否 | 迭代修订 | 自动停止 | 8B critic | 修订完整回答，不是中间状态动作路由 |
| 本文收窄版 | 是 | **严格同状态、查询、文档** | KEEP/APPEND/REVISE | **显式对→错风险** | 1.5B–3B LoRA-SFT | 目标是受控诊断 + 预防性轻量路由 |

## 8. 证伪条件

出现任一情况，就不应继续把路由器作为主方法：

1. 在至少 200 个独立问题上，动作 oracle 相对最强固定策略的 F1 增益小于 0.03。
2. LOCAL-REVISE 在等计算预算下不优于 FULL-RESTART，且伤害率也不低。
3. LoRA-SFT 路由器在独立测试集上不能超过同验证流程调参的 CDD-α/规则门控。
4. 跨数据集或跨触发器后效果消失，证明它只是 ETC/HotpotQA 特例。
5. 所谓“证据利用率”与最终正确性无稳定关系，无法支撑第一章到第二章的逻辑链。

若 1 或 2 失败，仍可保留“受控证据利用诊断”作为实证章节，并把方法章节降级为负结果或简单安全门控，不强行训练复杂模型。

## 9. 检索说明与完整性限制

- 检索范围：2023–2026，优先 2026、2025。
- 使用三组互补检索式：证据利用诊断、知识/上下文冲突、局部修复/干预风险。
- 多库初检得到 91 条去重记录，之后按题目摘要筛选并深读最直接工作。
- OpenReview 本地客户端缺失，改用官方网页核验；Semantic Scholar 两组查询触发 429；OpenAlex 有 504 重试。核心结论均回到官方论文页、ACL Anthology、NeurIPS proceedings、OpenReview 或 arXiv PDF 逐项核验，未依据聚合站摘要定论。

## 10. 结论

这件事“有人做过”，所以研究意义成立；也正因为有人做得很近，必须诚实收窄：

- 第一章贡献不是发现“证据未利用”，而是把它放进动态 RAG 的同状态、同证据算子实验中，做严格因子归因。
- 第二章贡献不是发明修复或反事实，而是在未知终局时用轻量成对监督做风险校准动作路由。
- 这条路线无需 RL；主训练只需 LoRA-SFT。完整 DPO、RL 和 GuarantRAG 类训练不列入毕业必做项。
