# 实验方案 v1：风险校准的检索后干预路由

更新日期：2026-07-22

## 1. 研究目标

本方案回答三个递进问题：

- **RQ1：证据到达后是否被利用？** 当检索结果已包含 gold supporting sentence 时，最终回答利用该证据并获得收益的比例是多少？
- **RQ2：干预算子是否造成独立因果差异？** 固定问题、中间状态、查询、Top-k 文档和解码设置，仅改变 KEEP、APPEND、LOCAL-REVISE、FULL-RESTART，结果是否显著不同？
- **RQ3：轻量路由是否有实际价值？** LoRA-SFT 路由器能否在不增加正确→错误污染的前提下，超过最强固定策略和阈值门控？

## 2. 核心假设

- **H1**：支持证据命中率显著高于证据利用率；命中支持句并不保证答案改善。
- **H2**：在同状态、同查询、同文档下，REVISE/RESTART 平均优于 APPEND，说明已生成前缀造成独立锚定。
- **H3**：没有单个干预算子在所有样本上占优，且强干预存在非零正确→错误风险，因此存在路由空间。
- **H4**：用部署时可见特征训练的 LoRA-SFT 路由器，在相同干预覆盖率或相同伤害上限下优于固定策略与规则门控。

## 3. 总体阶段

### P0：协议与已有结果审计（现在启动）

数据：已有 HotpotQA 20 个 qid 的 dense APPEND、gold APPEND、gold RESTART、真实 BM25 RESTART。

任务：

1. 以 `(qid, state_id, original_action_id)` 严格连接 APPEND 与 RESTART。
2. 审计问题、状态 token 前缀、查询文本、文档 ID/排名/正文完全一致。
3. 输出 KEEP/APPEND/RESTART 的逐动作配对表。
4. 按 qid 聚类 bootstrap，而不是把多个查询动作当成独立样本。
5. 统计 F1 差、Accuracy 错→对、对→错、最佳动作分布。

通过条件：所有配对文档一致；若不一致，停止使用旧结果比较，重新运行对应动作。

### P1：受控诊断主实验

数据规模：

- HotpotQA：开发集固定 200 qid；协议稳定后扩到 500。
- 2WikiMultiHopQA：200 qid。
- MuSiQue：200 qid；若 ETC 适配成本过高，可作为毕业扩展而非硬门槛。

状态采样：

- 第一 ETC 触发点。
- 第一完整句边界。
- 答案前最近句边界。
- DRAGIN 首次触发点，用于跨触发器验证。

状态预算：每题最多 3 个状态。先保证 qid 数量，不用一个问题堆很多相关动作来制造虚假样本量。

证据条件：

1. 真实 BM25 Top-3。
2. Dense Retriever Top-3（只在 BM25 协议稳定后加入）。
3. Gold supporting facts，仅作诊断上界。
4. 对 50–100 个样本构造冲突/干扰证据，复用 CDD 的实体、时间、逻辑和 distractor 类型。

动作：

- `KEEP`：复用 canonical 无检索续写。
- `APPEND`：保持 ETC 当前做法，在现有前缀后注入证据/桥接句并续写。
- `LOCAL-REVISE`：回退到最近句界；保留更早前缀，把证据放入可见上下文后重新生成后缀。
- `FULL-RESTART`：丢弃生成前缀，只保留问题、示例和同一检索证据重新生成。

解码控制：主实验使用 greedy/temperature 0；如框架存在不可避免随机性，对关键表格重复 3 个种子。各动作保持相同最大生成 token。

### P2：零训练与轻量路由

先做无需训练的基线：

1. Always KEEP。
2. Always APPEND。
3. Always LOCAL-REVISE。
4. Always FULL-RESTART。
5. CDD-α 风格冲突阈值门控。
6. 逻辑回归/LightGBM 小特征基线。
7. Oracle：读取真实终局分数选最佳动作，仅作为上界。

再训练 LoRA-SFT 路由器：

- 基座：优先 Qwen2.5-1.5B/3B-Instruct 或同量级本地可用模型。
- 输入：问题、有限长度前缀、检索查询、Top-k 证据、当前答案候选，以及部署时可计算的数值特征。
- 输出：结构化 JSON，包括 `action`、`predicted_gain_bin`、`harm_risk_bin`、`evidence_sufficiency`、`conflict`。
- 标签：来自真实执行各动作后的 paired outcome；不使用只由教师臆测的标签替代终局结果。
- 训练：LoRA-SFT；不做全量 DPO，不做 RL。LoRA-DPO 仅在 SFT 已稳定且时间充足时作为非必需附录。

动作标签不简单取 argmax。建议在训练集内定义：

`utility(a) = answer_F1(a) - λ * I(correct→wrong) - μ * normalized_token_cost(a)`

其中 `λ` 在验证集上按伤害上限选择，`μ` 只作次要成本项。测试集不得重新调阈值。

部署策略：只有当预测增益高于阈值且伤害风险低于阈值时才干预，否则 KEEP。报告完整风险—覆盖曲线，不只报告一个手选阈值。

### P3：泛化与消融

- 跨数据集：HotpotQA 训练，2Wiki 测试；再做少量 2Wiki LoRA 适配。
- 跨触发器：ETC 状态训练，DRAGIN 状态测试。
- 跨检索器：BM25 训练，dense 检索测试。
- 消融：去掉前缀、去掉证据、去掉冲突特征、去掉伤害头、去掉 KEEP 动作、全重启替代局部修订。
- 算力消融：1.5B 与 3B 路由器；若 1.5B 已足够，不扩大模型。

## 4. 指标

### 最终任务指标

- Answer EM、F1、Accuracy。
- 每 qid 最终得分；主要统计单位始终是 qid。

### 证据利用指标

- Gold title hit、supporting sentence hit。
- RAGChecker context utilization。
- `utilization given arrival`：支持句命中条件下，回答包含受支持金标 claim 的比例。
- `gain given arrival`：支持句命中条件下，动作相对 KEEP 的正/零/负收益。

### 安全与决策指标

- Wrong→Correct rate。
- Correct→Wrong rate，作为主风险指标。
- Intervention coverage。
- Risk—coverage curve 和在固定风险上限下的 F1。
- 相对最强固定策略的 regret；相对 oracle 的 oracle gap。
- 每题生成 token、检索次数、墙钟时延和峰值显存。

## 5. 统计方案

- 所有主比较均按 qid 做 paired bootstrap 95% CI，至少 10,000 次重采样。
- 二元错→对/对→错比较使用配对 McNemar 检验。
- 多个动作或多个数据集的同族检验使用 Holm 校正。
- 一个 qid 的多个状态/查询只作为簇内观测，不当作独立样本。
- 先锁定主指标：HotpotQA 测试上的 F1、Correct→Wrong、固定风险上限下的 coverage。其余为次指标，避免挑最好看的数字。

## 6. 数据划分与泄漏控制

- 以 qid 分组划分，任何同一问题的状态、查询、证据和动作必须落在同一 split。
- 第一版固定 60% train、20% dev、20% test；正式规模扩大后用固定 qid 清单版本化。
- Oracle 动作结果只可用于生成训练标签和最终上界；测试时路由输入不得包含终局分数、gold supporting facts、gold answer 或动作后的生成结果。
- Gold 证据条件只用于诊断，不与真实检索主结果混报。

## 7. 基线实现优先级

### P0 必做

- ETC APPEND、KEEP、FULL-RESTART。
- 同状态配对比较器。
- RAGChecker context utilization。
- CDD-α 风格门控。

### P1 必做

- LOCAL-REVISE。
- 规则/逻辑回归门控。
- LoRA-SFT 路由器。
- DRAGIN 跨触发器验证。

### P2 有余力再做

- RetroRAG、Astute RAG。
- AlignRAG 公布 critic。
- Doctor-RAG 官方代码若发布后的完整复现。

GuarantRAG 的全量 DPO/联合解码不列入毕业实验。

## 8. 算力与时间预算

服务器为 8×RTX 3090 24GB。建议：

- 数据 rollout：2 个 worker，每个 3 GPU，留 2 GPU 给检索服务、故障余量或小模型训练。
- 1.5B/3B LoRA-SFT：单卡或双卡即可；先用 1.5B 冒烟，再决定是否上 3B。
- 每次正式运行先做 1 qid，再做 20 qid，再扩大到 200；每阶段输出不可覆盖、带 manifest 的独立目录。
- 不删除旧结果，不复用不同 git commit/config 的运行目录。

## 9. 里程碑与停止条件

### M0：协议完成

- 现有 APPEND/RESTART 成对表通过四项一致性审计。
- CPU 单测全绿。

### M1：章节可成立

- 至少 200 HotpotQA qid。
- 证据到达/利用漏斗稳定存在。
- 至少两个干预算子在不同样本子集占优，oracle 相对最强固定策略 F1 ≥ 0.03。

### M2：方法可成立

- 路由器超过在同一 dev 集调参的 CDD-α 或逻辑回归基线。
- 在不高于最强固定干预 Correct→Wrong 的条件下提高 F1，或在相同 F1 下显著降低伤害/成本。

### Kill switch

- 若动作 oracle 增益 < 0.03：停止复杂路由训练，保留诊断章节。
- 若 LOCAL-REVISE 不优于 FULL-RESTART：删除局部修订主张，以 KEEP/APPEND/RESTART 安全选择收尾。
- 若 LoRA-SFT 不超过简单门控：不继续扩大模型，把结论写成“简单风险门控已足够”。
- 若跨 ETC 后失效：明确限定 ETC 场景，不声称通用动态 RAG。

## 10. 当前已经启动的工作

- 新增 `compare_interventions.py`：连接现有 APPEND 与真实证据 RESTART 结果，强制检查同样本、同状态、同查询来源和同文档签名。
- 新增单元测试：验证正确→错误统计，并确保文档不一致时直接拒绝比较。
- 下一步：提交并推送该分析器；服务器 `ff-only pull` 后，在已有 20 qid 两组分片上生成第一版配对报告。该报告通过后再实现 LOCAL-REVISE，避免在不可靠标签上训练路由器。
