# 2026-06-03 TODO（本轮结论）

## 本轮定下来的事

- **方向不变**：C'（第一章 DocRE on Re-DocRED 改进 TTM-RE；第二章 JERE on Re2-DocRED）。
- **两章串联主线锁定 = 关系假阴性（relation-level FN）**：
  - 第一章 = **模型侧**打 FN（在 TTM-RE 上做 FN 鲁棒的关系表示学习，不靠更好数据）。
  - 第二章 = 在**更难设定（实体未知 JERE）+ FN 更少的数据（Re2-DocRED）**上验证同一思想。
- **先做第一章，第二章招牌延后**：实体 FN / 模型×数据叠加性 等说法**目前都没数据支撑**，等复现 + 诊断实验后再定。
- **开题对外只讲骨架那一层**，细节统一答"开题后第一批诊断实验要测的"。

## 立即要做（按顺序）

1. **去实验室服务器跑 P0：复现 TTM-RE on Re-DocRED**（见 `05-next-steps.md` P0 checklist）
   - 目标：F1/Ign-F1 对得上论文 Table（±0.3 内）。
   - 省时技巧：用作者放出的 pretrain 权重直接跑 Stage 2，先复现 H+D 数字，不必从 distant 重训（见 `notes-ttmre.md` §7）。
2. **复现成功后，第一章改进**：在 entity-pair 表示上做 **FN 感知对比学习**（关键 novelty：不盲推疑似 FN 的 pair，区别于普通 InfoNCE / SAIS / AMTL）。
3. **诊断实验（决定第二章招牌）**：在 Re2-DocRED 跑一个 JERE baseline（AutoRE 或先 pipeline），分解漏掉的 triplet 里"实体没识别出来" vs "实体对了关系判错"的占比。
   - 实体漏召回占比**大** → 第二章可升级为"实体 FN"招牌。
   - 占比**小**（更可能）→ 第二章用"关系 FN 在 JERE 难设定下的迁移 + 模型侧×数据侧 FN 是否叠加"。

## 待查（不阻塞主线）

- **DocKS-RAG / EP-RSR / DRELL** 还没精读，确认它们占的地盘——尤其 DocKS-RAG 已经在做 "RAG + DocRE"，用户原"Retrieval-Augmented DocRE"标题包装需避开/区分。
- AutoRE PDF 已下载（`papers/AutoRE_LLM_DocRE_2024_ACL_Demo.pdf`），细读 RHF 范式再定第二章对接方式。

> 详细论证、三处纠正（对传播鲁棒 / 实体 FN / 补干净）全部记在 `02-papers.md` 的 Re2-DocRED / AutoRE / FN 主线区块。


